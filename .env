# API URL configuration
# For local development with backend: VITE_API_URL=http://localhost:5000/api
# For production (deployed on same server): leave empty or use relative path
# The app defaults to /api (relative URL) when this is not set
VITE_API_URL=
